<?xml version="1.0" encoding="utf-8" ?>
<mopups:PopupPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:mopups="clr-namespace:Mopups.Pages;assembly=Mopups"
    x:Class="MeuClienteApp.Views.Mopups.ConfirmacaoPopup"
    Title="Confirmação"
    CloseWhenBackgroundIsClicked="False"
    BackgroundColor="#80000000">

    <mopups:PopupPage.Resources>
        <ResourceDictionary>
            <Style x:Key="DialogButton" TargetType="Button">
                <Setter Property="Padding" Value="0" />
                <Setter Property="HeightRequest" Value="35" />
                <Setter Property="MinimumHeightRequest" Value="1" />
                <Setter Property="WidthRequest" Value="140" />
                <Setter Property="CornerRadius" Value="6" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
                <Setter Property="FontSize" Value="15" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="BackgroundColor" Value="#748085" />
                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup Name="CommonStates">
                            <VisualState Name="Normal" />
                            <VisualState Name="Pressed">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="#5a6b70" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>
        </ResourceDictionary>
    </mopups:PopupPage.Resources>

    <Border
        StrokeThickness="0"
        BackgroundColor="White"
        HorizontalOptions="Center"
        VerticalOptions="Center">

        <Border.StrokeShape>
            <RoundRectangle CornerRadius="12"/>
        </Border.StrokeShape>

        <VerticalStackLayout
            VerticalOptions="Center"
            HorizontalOptions="Center"
            Spacing="0"
            BackgroundColor="Transparent"
            WidthRequest="500">

            <Image Source="frame_superior_clientes.png"
                   Aspect="Fill"
                   HeightRequest="10"
                   WidthRequest="500">
                <Image.Clip>
                    <RectangleGeometry Rect="0,0,500,11" />
                </Image.Clip>
            </Image>

            <!-- Conteúdo -->
            <Grid Padding="30,40"
                  BackgroundColor="White"
                  ColumnSpacing="20"
                  RowSpacing="30"
                  WidthRequest="500">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0"
                       Grid.Column="0"
                       Grid.ColumnSpan="2"
                       x:Name="MensagemLabel"
                       Text=""
                       FontFamily="OpenSansSemibold"
                       TextColor="#072AC8"
                       FontSize="16"
                       HorizontalOptions="Center"
                       HorizontalTextAlignment="Center"
                       VerticalTextAlignment="Center"
                       LineBreakMode="WordWrap"
                       WidthRequest="430"
                       MaximumWidthRequest="430" />

                <Button Grid.Row="1"
                        Grid.Column="0"
                        x:Name="NaoButton"
                        Text="Não"
                        Clicked="OnNaoClicked"
                        BackgroundColor="#859EB7"
                        Style="{StaticResource DialogButton}"
                        HorizontalOptions="End" />

                <Button Grid.Row="1"
                        Grid.Column="1"
                        x:Name="SimButton"
                        Text="Sim"
                        Clicked="OnSimClicked"
                        BackgroundColor="#1E96FC"
                        Style="{StaticResource DialogButton}"
                        HorizontalOptions="Start" />
            </Grid>

            <!-- Rodapé -->
            <Image Source="frame_inferior_clientes.png"
                   Aspect="Fill"
                   HeightRequest="10"
                   WidthRequest="500">
                <Image.Clip>
                    <RectangleGeometry Rect="0,0,500,17" />
                </Image.Clip>
            </Image>
        </VerticalStackLayout>
    </Border>
</mopups:PopupPage>
